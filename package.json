{
  "name": "websocket-ping-check",
  "version": "0.1.0",
  "license": "MIT",
  "bin": "./out/cli.js",
  "scripts": {
    "start": "npm run build && node out/cli.js",
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/express-ws": "^3.0.0",
    "@types/node": "^14.10.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-ws": "^4.0.0"
  }
}
